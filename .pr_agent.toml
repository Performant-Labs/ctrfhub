# PR-Agent configuration for CTRFHub
#
# Docs: https://qodo-merge-docs.qodo.ai/usage-guide/configuration_options/
# Model IDs: https://docs.litellm.ai/docs/providers/deepseek  ← verify before first run; update model/fallback fields if names have drifted
#
# Default model: DeepSeek V4 via DeepSeek API.
# Fallback: DeepSeek V4 (same model).
# Override per-PR: add the `high-stakes` label to force DeepSeek V4 primary (see .github/workflows/pr-review.yml).

[config]
# Model IDs verified against LiteLLM DeepSeek provider docs 2026-05-02
# Auth: DEEPSEEK_API_KEY in GitHub Actions secrets; LiteLLM natively routes deepseek/deepseek-chat.
# DeepSeek V4 — LiteLLM routes natively.
model = "deepseek/deepseek-chat"
fallback_models = ["deepseek/deepseek-chat"]
verbosity_level = 1
# Don't truncate our skills/ context — reviews need the full spec context loaded
max_description_tokens = 1500
max_commits_tokens = 1500
# DeepSeek V4 is newer than PR-Agent's internal MAX_TOKENS table — set explicitly.
custom_model_max_tokens = 131072

[pr_reviewer]
require_score_review = true
require_tests_review = true
require_security_review = true
require_focused_review = false
num_code_suggestions = 4
inline_code_comments = true
automatic_review = true
persistent_comment = true
final_update_message = false
extra_instructions = """
You are the SPEC-ENFORCER AGENT for CTRFHub. Your job is drift detection, not code improvement.

MANDATORY CONTEXT — read before scoring:
1. CLAUDE.md at repo root — project overview, forbidden patterns, review priorities.
2. agents.md — the Spec-enforcer role definition. Follow it exactly.
3. Every file in skills/ — our conventions, each cites its source in docs/planning/.
4. docs/planning/*.md — authoritative spec. Do not suggest changes to these files.
5. docs/planning/tasks.md — identify which story this PR implements; check its declared test tiers and acceptance criteria.

PRIMARY JOB — spec drift detection:
1. Does the diff honor every applicable skill in skills/? For each violation, quote the skill filename and the rule it breaks.
2. Does the diff satisfy the story's acceptance criteria in docs/planning/tasks.md? Missing criteria = BLOCKING.
3. Are the declared test tiers (unit / integration / e2e) present in the diff? Missing required tests = BLOCKING.
4. For UI stories: are Tier 1 (Headless), Tier 2 (ARIA), and Tier 3 (Visual) assertions present per the story's declaration in docs/planning/tasks.md? Tier 3 present without Tier 2 = BLOCKING.
5. Any pattern in CLAUDE.md's "Forbidden patterns" list = BLOCKING. Always cite the triggering skill.

SECONDARY JOB — quality:
- Security issues (auth bypass, injection, unsafe deserialization, missing rate-limit, missing CSRF).
- Runtime regressions likely to slip past unit tests (race conditions, connection leaks, unbounded queries).

REVIEW STYLE:
- Distinguish BLOCKING (spec drift, forbidden pattern, missing required tests, security) from NIT (style, preference).
- Every BLOCKING finding MUST cite the skill file or spec section it derives from. No citation = downgrade to NIT.
- Do not suggest rewrites or alternative architectures. This repo follows the spec deliberately.
- If something in the diff is not covered by any skill or spec, say so explicitly and recommend the author flag it in docs/planning/gaps.md.
- When in doubt between blocking and nit, prefer nit — false positives erode trust in the reviewer.

OUTPUT SHAPE:
- Lead the review with a one-line verdict: BLOCKING / NITS / CLEAN.
- Then the sections PR-Agent normally produces.
- Inline comments should focus on the exact lines that violate a cited rule.
"""

# [pr_code_suggestions] is no longer auto-triggered (see [github_action_config]
# below — auto_improve = "false"). The section is retained to configure manual
# `/improve` invocations from PR comments. Default review now runs only the
# spec-enforcer pass, which already produces inline citation-backed comments —
# saving one full Kimi round-trip per PR.
[pr_code_suggestions]
num_code_suggestions = 4
summarize = true
extra_instructions = """
Only suggest code changes that bring the diff into compliance with skills/*.md or docs/planning/*.md.
Every suggestion must cite its source (skill filename or spec section).
Do not suggest refactors, renames, or alternative patterns that are not required by the spec.
"""

[pr_description]
publish_description_as_comment = false
add_original_user_description = true
keep_original_user_title = true
extra_instructions = """
If the PR description references a story ID from docs/planning/tasks.md, keep that reference prominent.
Append a one-line summary of the declared test tiers and whether each is present in the diff.
"""

[github]
publish_inline_comments = true
publish_output = true
publish_output_progress = false

[github_action_config]
# auto_review (= true by default) keeps the spec-enforcer pass firing on every
# pr-action event in `pr_actions` (see below). We turn auto_improve OFF: it
# was a second full model round-trip producing /improve-style suggestions that
# overlapped with the reviewer's inline comments, contributing meaningfully
# to the wall-time pressure that timed out PR #7's review at 15 min. Manual
# `/improve` from a PR comment still works for the rare case it adds value.
# auto_describe (= true by default) is left as-is — its run is short and its
# output (spec-aware PR description rewrite) is genuinely useful.
auto_review = true
auto_improve = false

# pr_actions explicitly includes "synchronize" so amends and rebases get a
# fresh review. PR-Agent's default omits synchronize, which silently skips
# the review on every force-push (observed: 6-second pr-agent steps that
# return success without producing any review content — see PR #7's rebase,
# PR #9's amend, PR #11's later runs, and PR #12's first force-push). The
# default ["opened", "reopened", "ready_for_review", "review_requested"]
# means the very first review on a PR fires correctly, but every subsequent
# push is a silent no-op. Without "synchronize" here, every rebase requires
# a manual `/review` comment to actually re-evaluate the diff.
#
# Source verified at qodo-ai/pr-agent@0e37fc8 in
# pr_agent/servers/github_action_runner.py L112.
pr_actions = ["opened", "reopened", "ready_for_review", "review_requested", "synchronize"]

[litellm]
# Let LiteLLM retry on transient failures before falling back
drop_params = true
# Needed for models not in LiteLLM's built-in token count table
max_model_len = 131072

[ignore]
# Files excluded from PR-Agent's review context. These don't get sent to the
# model, which speeds up review and reduces token cost. Use sparingly — too
# many excludes and a real bug can hide in a "boring" file.
#
# Categories:
#   * Generated lockfiles — regenerable from manifests; multi-thousand-line
#     diffs on dependency changes drove PR #7 against the 15-min timeout. The
#     manifests themselves (package.json, etc.) still get reviewed.
#   * Build outputs — `dist/`, `coverage/` are pure artifacts, never authored.
#   * Minified bundles — unreadable by the model and by humans.
#   * Snapshot fixtures — Vitest/Jest auto-generated; the test that produced
#     them is what gets reviewed.
glob = [
  'package-lock.json',
  'yarn.lock',
  'pnpm-lock.yaml',
  'dist/**',
  'coverage/**',
  '*.min.js',
  '*.min.css',
  '__snapshots__/**',
  '*.snap',
]
